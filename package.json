{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "Modern responsive portfolio website",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "concurrently \"Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force; cd server && npm start\" \"Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force; cd client && npm start\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy-rules": "firebase deploy --only firestore:rules",
    "deploy-all": "npm run build && firebase deploy"
  },
  "keywords": ["portfolio", "react", "express", "firebase"],
  "author": "Kiran Mahajan",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
